local function ExecuteScript()
    local success, result = pcall(function()
        loadstring(game:HttpGet("https://rawscripts.net/raw/V4-Blox-Loot-HOLDIK-HUB-57350"))()
    end)
    
    if not success then
        warn("Execution Error: " .. tostring(result))
    end
end

ExecuteScript()